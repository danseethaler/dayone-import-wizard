export default function(files) {
    console.log('files', files);
    // var filesPaths = Object.keys(files);
    // function importFile() {
    //     if (!filePaths.length) return;
    //
    //     var fileOb = files[filesPaths.shift()];
    //
    // }
    //
    // importFile();
}
